// Window Object : It represent browser window and provides method to control it.It is a Global Object
console.log(window);

// DOM : Document Object Model.
         // It represents the page content as HTML.
console.log(document.body);
document.body.style.background = "Cyan"

// BOM : Browser Object Model
         // The Browser Object Model represent additional object provided by the browser (host environment) for working with everything except the document.
         // The function alert/Confirm/prompt are also a part of the BOM.
location.href = "https://www.youtube.com/"
